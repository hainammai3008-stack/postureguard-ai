import Busboy from 'busboy';
import {requireAdmin,json} from './_shared.mjs';
const allowedModels=new Set(['cnn','resnet50','densenet121','efficientnetb0']);
function parseMultipart(event){return new Promise((resolve,reject)=>{const fields={},files=[],bb=Busboy({headers:event.headers});bb.on('field',(n,v)=>fields[n]=v);bb.on('file',(name,stream,info)=>{const chunks=[];stream.on('data',d=>chunks.push(d));stream.on('end',()=>files.push({fieldname:name,filename:info.filename,mimeType:info.mimeType,buffer:Buffer.concat(chunks)}))});bb.on('error',reject);bb.on('finish',()=>resolve({fields,files}));bb.end(Buffer.from(event.body||'',event.isBase64Encoded?'base64':'binary'))})}
function publicUrl(modelKey,version){const base=process.env.SUPABASE_URL,bucket=process.env.SUPABASE_MODEL_BUCKET||'ai-models';return `${base}/storage/v1/object/public/${bucket}/${modelKey}/${version}/model.json`}
export async function handler(event){
  try{if(event.httpMethod!=='POST')return json(405,{error:'Method not allowed'});const {db}=await requireAdmin(event);const {fields,files}=await parseMultipart(event),modelKey=String(fields.model_key||''),version=String(fields.model_version||'').trim(),activate=String(fields.activate||'false')==='true';if(!allowedModels.has(modelKey))return json(400,{error:'model_key không hợp lệ'});if(!/^[A-Za-z0-9._-]+$/.test(version))return json(400,{error:'model_version không hợp lệ'});const jf=files.find(f=>f.fieldname==='model_json'),bins=files.filter(f=>f.fieldname==='weight_files');if(!jf)return json(400,{error:'Thiếu model.json'});if(!bins.length)return json(400,{error:'Thiếu weight files *.bin'});if(jf.filename!=='model.json')return json(400,{error:'File JSON phải tên model.json'});for(const f of bins)if(!f.filename.endsWith('.bin'))return json(400,{error:`${f.filename} không phải .bin`});const bucket=process.env.SUPABASE_MODEL_BUCKET||'ai-models';for(const f of [jf,...bins]){const {error}=await db.storage.from(bucket).upload(`${modelKey}/${version}/${f.filename}`,f.buffer,{contentType:f.mimeType||'application/octet-stream',upsert:true});if(error)throw error}const model_url=publicUrl(modelKey,version);const {error:me}=await db.from('model_registry').upsert({model_key:modelKey,model_version:version,model_url,uploaded_at:new Date().toISOString()},{onConflict:'model_key,model_version'});if(me)throw me;let config=null;if(activate){config={id:1,selected_model:modelKey,model_version:version,model_url,updated_at:new Date().toISOString()};const {error:ce}=await db.from('system_config').upsert(config,{onConflict:'id'});if(ce)throw ce}return json(200,{ok:true,model_url,config})}catch(e){console.error(e);return json(e.statusCode||500,{error:e.message})}
}
